mirrors:
  docker.io:
    endpoint:
      - https://{{ settings['docker_registry']['domain'] }}:5000

configs:
  "{{ settings['docker_registry']['domain'] }}:5000":
    tls:
      insecure_skip_verify: true